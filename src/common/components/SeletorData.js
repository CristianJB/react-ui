import React from 'react';
import DatePicker from 'material-ui/DatePicker';

const SeletorData = () => (
    <div>
        <DatePicker hintText="Selecione a data" />
    </div>
);

export default SeletorData;